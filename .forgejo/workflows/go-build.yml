name: go build

on:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    uses: meta/workflows/.forgejo/workflows/go-build.yml@master
    runs-on: docker
    with:
      env_vars: 'GOEXPERIMENT=jsonv2'
